diff --git a/Cargo.toml b/Cargo.toml
index 1a2b3c4..5d6e7f8 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -10,6 +10,7 @@ edition = "2021"
 [dependencies]
 serde = { version = "1.0", features = ["derive"] }
 serde_json = "1.0"
+onto144 = { path = "../onto144", version = "0.1" }
 uuid = { version = "1.0", features = ["v7"] }
 clap = { version = "4.5", features = ["derive"], optional = true }
 tokio = { version = "1.0", features = ["full"], optional = true }
diff --git a/src/core/activity_ledger.rs b/src/core/activity_ledger.rs
index 8a9b0c1..2d3e4f5 100644
--- a/src/core/activity_ledger.rs
+++ b/src/core/activity_ledger.rs
@@ -1,15 +1,12 @@
 // SPDX-License-Identifier: GPL-3.0-only
 // OntoCMS Core — Activity Ledger (CRDT-Compatible, Phase-Aware)
 
-use serde::{Deserialize, Serialize};
-use std::collections::HashMap;
-
-/// Онтологический профиль по onto-144
-#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq, Hash)]
-pub struct ProfileId(String); // e.g., "Aries-Wood-Rabbit"
+use onto144::profile::Profile as ProfileId;
 
 /// Фазы онтогенеза
-#[derive(Serialize, Deserialize, Clone, Debug, PartialEq, Eq)]
-pub enum OntoPhase {
+#[derive(Clone, Debug, PartialEq, Eq)]
+pub use onto144::state::phase::Phase as OntoPhase;
+
+/*
     Fast,     // Реактивный режим
     Decline,  // Деконструкция
     Slow,     // Рефлексивный, NoemaSlow
@@ -17,7 +14,7 @@ pub enum OntoPhase {
     Heyday,   // Генеративный синтез
-}
+*/
 
 /// CRDT-совместимое событие
 #[derive(Serialize, Deserialize, Clone, Debug)]
diff --git a/src/forms/invariant_registry.rs b/src/forms/invariant_registry.rs
index 3f4g5h6..7i8j9k0 100644
--- a/src/forms/invariant_registry.rs
+++ b/src/forms/invariant_registry.rs
@@ -1,120 +1,40 @@
 // SPDX-License-Identifier: GPL-3.0-only
 // OntoCMS Forms — Canonical Hash Registry for onto-144
 
-use std::collections::HashMap;
-use std::fs;
-use std::path::Path;
-
-use serde::{Deserialize, Serialize};
-
-#[derive(Serialize, Deserialize, Clone, Debug)]
-pub struct Onto144Profile {
-    pub id: String,
-    pub perceptual_modality: PerceptualModality,
-    pub behavioral_element: BehavioralElement,
-    pub invariant_hash: String,
-}
-
-#[derive(Serialize, Deserialize, Clone, Debug)]
-pub struct PerceptualModality {
-    pub zodiac_sign: String,
-    pub element: String,
-    pub modality: String,
-    pub traits: Vec<String>,
-    pub cognitive_bias: String,
-}
-
-#[derive(Serialize, Deserialize, Clone, Debug)]
-pub struct BehavioralElement {
-    pub chinese_element: String,
-    pub chinese_animal: String,
-    pub yin_yang: String,
-    pub season: String,
-    pub traits: Vec<String>,
-    pub decision_style: String,
-}
+use std::collections::HashMap;
+use onto144::profile::Profile;
 
 pub struct InvariantRegistry {
-    profiles: HashMap<String, Onto144Profile>,
+    profiles: HashMap<String, Profile>,
     hash_to_id: HashMap<String, String>,
 }
 
 impl InvariantRegistry {
     pub fn new() -> Self {
         Self {
             profiles: HashMap::new(),
             hash_to_id: HashMap::new(),
         }
     }
 
     /// Загрузка всех 144 профилей из onto144
     pub fn load_from_disk(&mut self, _profiles_dir: &str) -> Result<(), Box<dyn std::error::Error>> {
-        let paths = fs::read_dir(profiles_dir)?;
-        for entry in paths {
-            let entry = entry?;
-            let path = entry.path();
-            if path.extension() == Some(&std::ffi::OsStr::new("yaml")) {
-                let content = fs::read_to_string(&path)?;
-                let profile: Onto144Profile = serde_yaml::from_str(&content)?;
-                
-                // Проверка: нет запрещённых полей
-                if Self::contains_forbidden_fields(&content) {
-                    panic!("Profile {} contains forbidden energy/value fields", profile.id);
-                }
-
-                // Проверка хеша (в реальном коде — пересчёт)
-                if !Self::validate_hash(&profile) {
-                    panic!("Invalid invariant_hash in {}", profile.id);
-                }
-
-                self.hash_to_id.insert(profile.invariant_hash.clone(), profile.id.clone());
-                self.profiles.insert(profile.id.clone(), profile);
-            }
-        }
-
-        if self.profiles.len() != 144 {
-            panic!("Expected 144 onto-144 profiles, found {}", self.profiles.len());
-        }
+        let profiles = onto144::profile::generator::load_all_profiles("../onto144/profiles")?;
+        for profile in profiles {
+            self.hash_to_id.insert(profile.invariant_hash.clone(), profile.id.clone());
+            self.profiles.insert(profile.id.clone(), profile);
+        }
         Ok(())
     }
 
-    fn contains_forbidden_fields(content: &str) -> bool {
-        let forbidden = ["energy", "value", "score", "attract", "monetize"];
-        forbidden.iter().any(|kw| content.contains(kw))
-    }
-
-    /// Валидация хеша (в production — пересчёт без поля invariant_hash)
-    fn validate_hash(profile: &Onto144Profile) -> bool {
-        // Заглушка: в реальной системе — сериализация без `invariant_hash` + SHA3-256
-        // Здесь — просто проверка формата
-        profile.invariant_hash.starts_with("sha3-256:") && profile.invariant_hash.len() == 71
-    }
-
     pub fn get_profile(&self, id: &str) -> Option<&Profile> {
         self.profiles.get(id)
     }
 
     pub fn verify_hash(&self, hash: &str) -> Option<&str> {
         self.hash_to_id.get(hash).map(|s| s.as_str())
     }
 
     pub fn all_ids(&self) -> Vec<String> {
         self.profiles.keys().cloned().collect()
     }
 }
diff --git a/src/ontocoder/validator.rs b/src/ontocoder/validator.rs
index 9k0l1m2..3n4o5p6 100644
--- a/src/ontocoder/validator.rs
+++ b/src/ontocoder/validator.rs
@@ -1,7 +1,7 @@
 // SPDX-License-Identifier: GPL-3.0-only
 // OntoCoder — License and Ontology Validator
 
-use crate::core::activity_ledger::ProfileId;
+use onto144::profile::Profile as ProfileId;
 
 #[derive(Debug)]
 pub enum ValidationError {
diff --git a/docs/ONTO-144-ARCH.md b/docs/ONTO-144-ARCH.md
index 4p5q6r7..8s9t0u1 100644
--- a/docs/ONTO-144-ARCH.md
+++ b/docs/ONTO-144-ARCH.md
@@ -1,5 +1,5 @@
 # Роль onto-144 в архитектуре ontoCMS
 
-onto-144 — это онтологическая матрица из **144 уникальных ментальных профилей**, построенных на:
+onto-144 — это [отдельная библиотека](https://github.com/ontocms/onto144), предоставляющая онтологическую матрицу из **144 уникальных ментальных профилей**, построенных на:
 - **12 зодиакальных знаках** → определяют **модальность восприятия** (как сущность видит мир),
 - **5 китайских элементах + 4 темпераментах** → определяют **поведенческую активность** (как сущность действует).
 
@@ -15,12 +15,8 @@ onto-144 — это онтологическая матрица из **144 уникальных ментальных профилей*
     traits: ["гибкость", "уклонение", "внутренний диалог"]
     invariant_hash: "sha3-256:..."